# 电机控制上位机软件介绍

> 本文档是面向用户的精简介绍。完整架构、协议字节级规范、模型参数与上下行数据字典分别见：
> - [`README.md`](README.md) — 完整项目说明
> - [`下位机适配/PROTOCOL_V2.md`](下位机适配/PROTOCOL_V2.md) — v2 协议帧格式
> - [`数字孪生与电机参数.md`](数字孪生与电机参数.md) — 数字孪生模型参数表
> - [`通信规约与数据字典.md`](通信规与数据字典.md) — 上下行命令/遥测字段、PWM 比较值/采样点/扇区/VDDA 语义
> - [`使用说明书.md`](使用说明书.md) — 实验工作流与操作说明

## 一、软件定位

本软件是一套面向多类型电机的**模块化上位机控制与监测平台**，基于 Python + PySide6 构建，覆盖：

- **永磁同步电机（PMSM）** / **双凸极电机（SRM）** / **直线电机** 三类电机
- 实时监控、控制策略下发、数据采集
- 数字孪生仿真、参数辨识、矢量可视化
- AI 异常检测与边缘 AI 推理
- 可追溯实验工作流（模板/档案/事件标记/报告）

软件强调**模块化**与**可扩展性**：每个功能（页面、控制算法、通信协议、AI 模块）均独立成单独的 Python 文件，方便单独维护、测试与升级。

> 本软件是实验管理与运行辅助工具，不替代下位机保护、硬件限流、急停回路与高压安全连锁。500 V 平台的安全保护必须由独立硬件回路实现。

## 二、关键能力速览

| 能力 | 说明 |
|---|---|
| 电机类型 | PMSM / SRM / 直线电机，按类型动态切换控制方式 |
| 控制方式 | 闭环 PI、开环、MPC、无位置传感器（PMSM）；CCC/APC/电压 PWM（SRM） |
| 位置传感器 | 7 种多选（Hall/QEP/Resolver/SMO/EKF/MRAS/HFI），含元数据注册表 |
| 通信 | RS-232 / RS-485 / CAN / TCP，v1+v2 双协议，3 种 CAN 后端 |
| 数字孪生 | PMSM dq 轴物理模型，0.5 ms 步长，含母线动力学与一阶热模型 |
| 实验工作流 | 模板/不可变档案/运行状态机/事件标记/Markdown+HTML+SVG 报告 |
| 监督学习 | MLP/CNN/LSTM/Transformer/RF/SVM 6 种，含 ONNX 导出 |
| 强化学习 | PPO/SAC/TD3 + MPC 专家预热 + DAgger 在线纠正 |
| AI 集成 | OpenAI 兼容大模型 + 图片理解 + BM25 RAG 知识库 |
| 边缘推理 | ONNX Runtime 本地推理，无模型时回退规则检测 |

## 三、三通道遥测

上位机把遥测拆成三个独立通道，各有立频率与字节长度：

| 通道 | 命令 | 载荷字节 | 典型频率 | 用途 |
|---|---|---|---|---|
| 常规遥测 | 0xF0 | 15 / 8（CAN） | 10 Hz | 速度/温度/故障等慢变量 |
| 高速遥测 | 0xF1 | 12 / 16 / 22 | 200 Hz / 1 kHz（TCP） | 电角度/Iq/Iqref/相电流/Vd/Vq/Vbus |
| 电流采样诊断 | 0xF2 | 21 / 29 / 31 | 50 Hz | ADC 原始值/PWM duty/扇区/标定窗口 |

完整字段与字节布局见 [`下位机适配/PROTOCOL_V2.md`](下位机适配/PROTOCOL_V2.md)。

## 四、协议双版本

| 项 | v1（默认兼容老固件） | v2（实验工作流推荐） |
|---|---|---|
| 帧头/帧尾 | `0xAA` / `0x55` | `0xA5 0x5A` / `0x7E` |
| 校验 | 8 位累加和 | CRC16-CCITT-FALSE |
| 容量 | 单帧 ≤ 255 字节 | payload ≤ 4096 字节 |
| 握手 | 无 | HELLO/CAPABILITIES + 设备身份白名单 |
| 应答 | 无 | ACK/NACK + 序号 + 超时 |
| CAN 支持 | 8 字节裸数据 | **暂不支持**（需分片协议） |

v2 握手失败**不降级到 v1**，设备身份不符直接拒绝连接。

## 五、运行状态机

所有危险运行意图通过 `core/runtime_state.py` 的状态机统一约束：

```
DISCONNECTED → CONNECTED → PRECHECK → READY → RUNNING → STOPPING → READY
                                            │
                                  FAULT_LOCKED ──reset_fault(人工)──> CONNECTED/DISCONNECTED
```

`FAULT_LOCKED` **跨重连保持**，必须显式复位；RUNNING/STOPPING 期间断线立即 `FAULT_LOCKED`，防止重连就自动清故障。

## 六、技术栈

| 类别 | 选型 |
|---|---|
| GUI 框架 | PySide6 (Qt 6) |
| 绘图 | pyqtgraph |
| 串口 | pyserial |
| CAN | python-can / 周立功 ControlCAN.dll / zlgcan.dll |
| 深度学习 | PyTorch |
| 经典 ML | scikit-learn |
| 推理 | ONNX Runtime |
| 数据 | NumPy / pandas |
| 知识库 | BM25 本地检索 + pypdf + rapidocr_onnxruntime（可选） |
| AI 客户端 | OpenAI 兼容 HTTP 接口 |
| 打包 | PyInstaller 单文件 |

## 七、安装与运行

```bash
# 运行依赖（Python 3.10+）
pip install -r requirements.txt

# 训练依赖（含 torch，体积较大，可选）
pip install -r requirements-train.txt

# 启动
python main.py
```

首次启动默认进入监控页面，`CommManager` 自动注入模拟遥测，无需连接下位机即可调试 UI。

## 八、相关文档

- [`README.md`](README.md) — 完整项目说明、架构树、扩展指南、常见问题
- [`使用说明书.md`](使用说明书.md) — 实验工作流、保护边界、真机连接、监控/矢量/功率流、异常处理
- [`下位机适配/PROTOCOL_V2.md`](下位机适配/PROTOCOL_V2.md) — v2 协议帧格式、握手、ACK、心跳
- [`下位机适配/README.md`](下位机适配/README.md) — v1 协议下位机适配指南
- [`数字孪生与电机参数.md`](数字孪生与电机参数.md) — PMSM 模型参数、PI 增益、母线/热模型
- [`需求文档.txt`](需求文档.txt) — 当前实现对照原始需求

---

**软件版本**：v1.8.0
**最后更新**：2026-07-20