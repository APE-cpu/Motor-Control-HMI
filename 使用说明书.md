# 电机控制上位机使用说明书

> 适用于当前实验工作流版本。本软件是实验管理和运行辅助工具，不替代下位机保护、硬件限流、急停回路和高压安全连锁。

## 1. 启动与界面

在项目根目录运行：

```powershell
python main.py
```

左侧导航分为：

- 运行控制：监控页面、电机控制、实验管理。
- 分析可视化：矢量可视化、功率流、参数辨识。
- AI智能：AI分析、边缘AI和可选的模型训练。
- 系统：通信设置、操作记录和本使用说明书。

## 2. 数字孪生实验标准流程

如果只想直接观看仿真波形，在“监控页面”设定目标转速后点击“快速仿真演示”。软件会自动建立数字孪生环境、完成仅限仿真的基础边界检查并启动电机。该入口在真机连接和故障锁定时禁用，也不会伪装成已完成正式实验预检。

如果要保留完整实验记录，则使用下列标准流程：

1. 进入“电机控制”，选择电机、位置传感器和控制方式。
2. 设定最高转速、电流限幅、额定工作点温度和控制参数，点击“保存/应用参数”。
3. 进入“监控页面”，点击“启动仿真”。这只建立数字孪生数据源，不等于启动电机。
4. 进入“实验管理”，填写名称和目的，数据源选“数字孪生”。
5. 选择并应用设备档案；78 W低压仿真可使用内置档案。根据需要应用实验模板。
6. 点击“新建并开始实验”，从此时起归档遥测、操作和标记。
7. 点击“执行运行预检”；只有进入`READY`后才能启动电机。
8. 回到“监控页面”，点击“启动”。运行中可调速、投卸负载，并在实验页记录时间标记。
9. 结束时先点击“停止”，等待返回`READY`，再在实验页点击“正常结束”。
10. 最后点击“停止仿真”。模型、遥测、角度表和矢量图会复位到静止初始状态。
11. 在历史实验中查看曲线，编辑结论后生成Markdown/HTML报告。

## 2.1 小屏幕与窄窗口

所有主页面都位于可滚动容器中。当窗口宽度不足时，软件保留输入框、曲线和按钮的最小尺寸，并提供水平/垂直滚动条，而不是继续压缩到重叠。监控页的标题/报告区和运行操作区已拆成两行。左侧导航允许在130–170 px之间收缩。

## 3. 电机控制与保护边界

### 3.1 电流限幅

“电流限幅”限制数字孪生速度环输出的q轴电流给定，同时以`max_current_a`随参数帧下发并冻结到实验保护快照。预检会拒绝当前电流已越界的启动。

> 该值不是安全认证的硬件限流。真机必须在下位机实现独立的电流给定限制、过流比较器、驱动保护与封波逻辑。500 V平台不得仅依赖上位机。

### 3.2 温度

- 电机控制页的“额定工作点温度”是设备档案/铭牌属性，不随遥测变化。
- 监控页面的“电机实际温度”来自数字孪生或下位机遥测，用颜色显示温度等级。
- 设备档案的`max_temperature_c`是实验运行上限，与额定工作点温度含义不同。

## 4. 真机连接

1. 在“通信设置”选择串口/TCP参数。真实v2推荐选`negotiated-v2`。
2. 在实验页选择带真实设备ID、硬件版本和固件前缀的设备档案修订。
3. 握手身份不符时端口会关闭，且不降级到v1。
4. 真机实验仍按“开始归档 → 预检 → 启动 → 停机 → 结束归档”执行。

### 4.1 RS-485 / CAN带宽规划

上位机把遥测拆成三个独立通道，各自有独立的频率与字节长度（完整字段语义与 PWM 比较值/采样点/扇区/VDDA 说明详见 [`通信规约与数据字典.md`](通信规约与数据字典.md)，帧格式见 [`下位机适配/PROTOCOL_V2.md`](下位机适配/PROTOCOL_V2.md)）：

| 通道 | 命令 | 载荷字节 | 典型频率 | 单帧完整字节 |
|---|---|---|---|---|
| 常规遥测 | 0xF0 | 15（串口）/ 8（CAN） | 10 Hz | v2 ≈ 30 字节（含头尾+CRC），v1 串口 20 字节 |
| 高速测 | 0xF1 | 12 / 16 / 22 | 200 Hz（UART）/ 1 kHz（TCP 批量） | 27 / 31 / 37 字节 |
| 电流采样诊断 | 0xF2 | 21 / 29 / 31 | 50 Hz | 36 / 44 / 46 字节 |

带宽估算：

- 0xF0 通道 10 Hz × 30 B ≈ 300 B/s；
- 0xF1 通道 200 Hz × 31 B ≈ 6.2 kB/s；
- 0xF2 通道 50 Hz × 44 B ≈ 2.2 kB/s；
- 合计约 8.7 kB/s。RS-485 115200 baud（8N1）理论字节率约 11.52 kB/s，因此 0xF1 跑满 200 Hz 已接近 76% 占用率，再加 ACK/心跳余量紧张。
- 1000 Hz 0xF1 仅在以太网 TCP 上启用，单样本 37 B × 1000 = 37 kB/s，远低于 100 Mbps 以太网容量。
- 经典 CAN 每帧只有 8 字节 payload，**只能承载 v1 的 8 字节压缩遥测**（4 个核心字段），v2 尚未定义 CAN 分片协议，不应把完整 v2 帧搬到经典 CAN。
- 建议总线长期平均占用率控制在 30% 左右，为故障、ACK 和突发快照留出确定性余量。
- PWM/电流环级数据不连续上传：下位机保留 1–20 kHz 环形缓冲，上位机常态接收 50–200 Hz 摘要；异常时再触发上传故障前后快照。需要持续高速波形时应使用 CAN FD、隔离以太网/USB 或独立 DAQ。

#### 以太网建议

- 当前上位机已支持TCP和`negotiated-v2`，可用于参数、命令、ACK、状态摘要和快照文件；100 Mbps以太网的带宽足以覆盖常规实验数据。
- TCP保证有序与可靠，但重传会造成队首阻塞和延迟抖动，不用于PWM/电流环或安全保护。
- 高速容错遥测可增加UDP通道，帧中必须含设备ID、序号、单调时间戳、profile版本和CRC；丢包时不重传过期波形。TCP继续负责控制、配置和文件。
- Python/Qt不应逐点绘制所有高速数据；接收线程写入环形缓冲/文件，界面仅50–200 Hz取包络、统计或降采样。
- 500 V与强电磁干扰环境优先使用光纤以太网或符合系统绝缘设计的工业以太网。普通网口变压器不能自动被视为整机安全隔离。急停与封波仍必须是独立硬件回路。

## 5. 监控、矢量与功率流

### 5.1 监控页面

显示转速、电流、母线、转矩、角度、实际温度和传感器质量。温度区同时显示额定工作点、实际偏差和温度状态。角度表在低速直读机械角度，高速时是带转向的慢放示意。旁边显示`f_e=|n|·p/60`电频率：10 Hz遥测无法正确呈现例如100 Hz的电角轨迹，因为每次采样可能刚好跨过整数个电周期，产生频闪静止。真实电角轨迹应来自下位机高速突发快照，不由上位机低速采样伪造。原始计数仍保留在传感器诊断波形和实验遥测中。

### 5.1.1 电流采样诊断页（0xF2 通道）

电流采样诊断页独立于监控主曲线，按 50 Hz 接收 0xF2 帧，展示 ADC 注入采样原始值、零点偏置、SVPWM 扇区与三相 duty。每帧字段含义：

| 字段 | 含义 | 换算 |
|---|---|---|
| `adc1_raw` / `adc2_raw` | A/B 相 ADC 注入采样原始码 | `V = raw × 3.3/32768` |
| `offset_a` / `offset_b` | A/B 相零点（MCSDK 已翻倍） | `zero_v = (raw/2) × 3.3/32768` |
| `sector` | SVPWM 扇区 0..5 | 正常运行应循环 0..5 |
| `duty_a/b/c` | 三相占空比原始码 | 与 PWM 周期寄存器配套 |
| `cal_adc1_min/max` / `cal_adc2_min/max` | 校准窗口 | `cal_pp = max - min` 应大于实测电流 PP |
| `vdda_mv`（可选） | VREFINT 换算的实际 VDDA | 0 = 固件尚未完成首次测量 |

电流换算公式（写死在代码里）：`amps_per_mc_digit = 3.3 / (65536 × 0.01 × 8)`，其中 `0.01` 是采样电阻 Ω、`8` 是运放增益。**若下位机改动这两个硬件常量必须同步修改上位机解码常量**，否则 A 相电流读数会全错。

> STM32F4 注入组 ADC 采用 12 位左对齐至 16 位，bit15 用作符号位，有效范围 0..32760；MCSDK 把结果翻倍后再做 offset 减法，因此 `offset_*` 字段会出现大于 32768 的值（如 32840），这是协议特性不是溢出。

### 5.1.2 波形落盘

高速波形 CSV 落盘到 `波形记录/` 目录，文件名按 `波形_YYYYMMDD_HHMMSS.csv` 与 `电流采样诊断_YYYYMMDD_HHMMSS.csv` 区分。CSV 采用 `utf-8-sig`（带 BOM）编码，Excel 直接打开不乱码。波形 CSV 字段为长格式 `channel, time_s, series, value`，每个通道/每条曲线独立成行；电流采样诊断 CSV 字为宽格式，与 5.1.1 表格一致。

### 5.2 矢量可视化

仿真使用1 kHz dq轨迹；真机在没有突发快照通道时，仅能由低速遥测近似重构。停止仿真后余辉和矢量清空，不再使用演示角继续旋转。

### 5.3 功率流

能量链路右侧的“实时计算”栏将公式、当前功率和守恒诊断放在同一视线内。母线储能变化率为正表示母线电容正在充电，为负表示放电；机械平衡误差应近似为零。“电机储能/未建模项”包含瞬态磁场储能变化以及当前未单独建模的损耗，不应直接当成测量误差。

主要仿真计算为：

- 逆变器输入：`P_inv = 1.5(vd·id + vq·iq)`
- 定子铜损：`P_Cu = 1.5 Rs(id² + iq²)`
- 电磁功率：`P_em = Te·ωm`
- 动能变化率：`P_kin = P_em - P_fric/load`
- 电源输入与内阻损耗：`P_src = Vsrc·Isrc`，`P_src-loss = Isrc²Rsrc`
- 制动电阻：导通时`P_brake = Vdc²/Rbrake`

正值表示电源到转轴，负的逆变器/电磁功率表示回馈。当前忽略逆变器开关损耗、铁耗和杂散损耗；效率`P_em/P_src`仅适合稳态趋势。真机未上报dq电压/电流或功率字段时，页面不猜测功率。

## 6. 实验档案和报告

- 设备档案每次保存产生新修订，旧修订不会被覆盖。
- 实验开始时冻结设备、控制、保护、通信和模板快照。
- 运行中可记录负载投入、调速、振荡、异响和保护触发等时间标记。
- 结束后可查看历史曲线、编辑结构化结论并生成离线报告。

## 7. 异常处理

- 普通停机：使用“停止”，等待状态回到`READY`。
- 状态不明、过流、过压或机械异常：使用“紧急停止”，实验选“异常中止”。
- `FAULT_LOCKED`后不能直接重启。先切除功率和负载风险，确认下位机/驱动保护已复位，再在实验页执行“确认并复位故障”。
- 任何真机握手失败、身份不符或ACK超时都应先查看“通信设置”和“操作记录”，不要反复尝试带功率重启。
