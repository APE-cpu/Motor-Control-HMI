# 电机控制上位机软件使用说明书

> 适用版本：v1.3（含数字孪生虚拟电机、参数辨识、矢量可视化、AI 图片理解、周立功 USBCAN 双后端）

## 一、安装与启动

### 1.1 系统要求

- Windows 10 / 11
- Python 3.10 或更高版本
- 串口设备（用于 RS-232 / RS-485）或 CAN 适配器（用于 CAN 总线）—— 仅在接入真实下位机时需要

### 1.2 安装依赖

在项目根目录打开终端：

```bash
pip install -r requirements.txt
```

### 1.3 启动软件

```bash
python main.py
```

首次启动后默认停留在“监控页面”，并自动注入模拟数据，用于 UI 调试。

主窗口左侧为导航栏，自上而下依次为：

1. 监控页面
2. 电机控制
3. 矢量可视化
4. 参数辨识
5. 通信设置
6. AI 分析
7. 边缘 AI
8. 模型训练
9. 操作记录

底部状态栏显示当前通信连接状态。

---

## 二、监控页面

实时显示电机运行参数与曲线。

| 区域 | 内容 |
| --- | --- |
| 实时数据卡片 | 实际/给定转速、电流、转矩，以及实际角度 |
| 统计 | 转速 / 电流 / 转矩的最大、最小值 |
| 趋势曲线 | 三条曲线（实际 vs 给定）按时间滚动显示最近 100 点 |

无需操作，刷新周期 100 ms。如未连接下位机，会显示模拟数据。

---

## 三、电机控制页面

### 3.1 电机信息区
1. **电机类型**：从下拉框中选择 PMSM / 双凸极电机 / 直线电机；
   切换电机类型后，右侧“控制方式”下拉框会**自动刷新**为该电机可用的方式。
2. **电机型号**：自定义型号字符串。
3. **极对数 / 最高转速**：用于参数校验与电机限制保护。
4. **实际温度**：从下位机回传，正常温度显示绿色，超过 60 ℃ 变橙，超过 85 ℃ 变红。

### 3.2 位置传感器（可多选）

勾选你希望启用的传感器，**可同时勾选多个**：

- 霍尔传感器 (Hall)
- 增量式编码器 (QEP)
- 旋转变压器 (Resolver)
- 无位置-滑模观测器 (SMO)
- 无位置-扩展卡尔曼 (EKF)
- 无位置-模型参考自适应 (MRAS)
- 无位置-高频注入 (HFI)

> 典型组合举例：
> - **PMSM 调试场景**：勾选 QEP（主用）+ Hall（兜底）
> - **PMSM 量产无位置**：勾选 SMO + HFI（低速时切换）
> - **双凸极电机**：勾选 Hall 或 QEP

“已选：xxx”状态条会实时显示当前勾选项。如启动时未勾选任何传感器，软件会弹窗确认。

### 3.3 控制方式

#### PMSM / 直线电机

| 控制方式 | 关键参数 |
| --- | --- |
| 闭环 PI 控制 | Kp、Ki、Kd、采样时间 |
| 开环控制 | 幅值、频率、占空比 |
| 模型预测控制 (MPC) | 预测时域 N、控制时域 M、权重 Q/R、约束 u_min/u_max |
| 无位置传感器控制 | 观测器增益、估算方法（SMO/EKF/MRAS/HFI）、启动频率、启动电流 |

#### 双凸极电机（专属）

| 控制方式 | 关键参数 | 适用工况 |
| --- | --- | --- |
| **电流斩波控制 (CCC)** | 电流上/下限、斩波频率、滞环带宽 | **低速重载**，电流滞环维持平稳转矩 |
| **角度位置控制 (APC)** | 开通角 θ_on、关断角 θ_off、提前角、限流 | **中高速**，调节导通区间 |
| **电压 PWM 控制** | 母线电压、占空比、PWM 频率、电压限幅 | **宽调速**，结构最简单 |
| 模型预测控制 (MPC) | 同上 | 研究与高动态场景 |

切换控制方式时，下方“控制参数调整”区会自动切换为相应的参数面板。

### 3.4 控制操作

| 按钮 | 功能 |
| --- | --- |
| 启动电机 | 按当前目标转速 + 控制方式 + 传感器选项下发 `CMD_START` |
| 停止电机 | 下发 `CMD_STOP`，电机减速停机 |
| 保存/应用参数 | 把当前参数面板的值下发到下位机（`CMD_SET_PARAMS`），同时本地缓存 |
| 启动仿真 | 仅离线时可用，在本地生成模拟遥测，便于 UI 验证 |
| 紧急停止 | 红色高亮按钮，下发 `CMD_EMERGENCY_STOP`，并复位本地控制器内部状态 |

> 启动前会校验目标转速是否超过“最高转速”设定。

---

## 四、矢量可视化页面

实时绘制 αβ 平面上的 **电流圆 iα-iβ** 与 **磁链圆 ψα-ψβ**：

- 仿真模式：直接取虚拟电机 1 kHz 高速轨迹（真实 dq 变换，无频闪）
- 真机模式：由 10 Hz 遥测按 id≈0 假设重构（欠采样，轨迹靠余辉点云累积成圆）
- 勾选"无限余辉"可长时间累积轨迹，"清空余辉"重新开始
- 几何解读：正圆 = 正常；圆度 / 圆心异常 = 不平衡、偏心、退磁等征兆
- 电机参数（ψf、Lq 等）取自数字孪生配置，可在参数辨识页更新

---

## 五、参数辨识页面

通过两点稳态 + 滑行实验，仅用转速与电流遥测辨识
**B（粘滞摩擦）、Tc（库仑摩擦）、J（转动惯量）**：

1. 填写磁链 ψf（铭牌或反电动势实验获得）与极对数
2. 设置两个稳态转速点（默认 1500 / 2800 rpm）
3. 点击"开始辨识实验"，软件依次运行 稳态点 1 → 稳态点 2 → 滑行段，自动采集并求解
4. 辨识完成后点"应用到数字孪生"，矢量可视化页立即使用新参数

> 原理：稳态 `Kt·iq = B·ω + Tc`（两个转速点解出 B、Tc，Kt = 1.5·p·ψf）；
> 滑行 `J·dω/dt = −(B·ω + Tc)`（最小二乘拟合 J）。
> 仿真模式下可与虚拟电机真值对比，验证辨识精度。

---

## 六、通信设置页面

### 6.1 选择通信方式
顶部下拉框选择 RS-232 / RS-485 / CAN 总线。

### 6.2 串口（RS-232/485）参数
- **端口号**：从下拉框选择已枚举的 COM 口，或手动输入
- **波特率**：9600 / 19200 / 38400 / 57600 / 115200 / 230400
- **数据位**：固定 8
- **停止位**：1 / 2
- **校验位**：无 / 奇 / 偶
- **超时**：单位毫秒

### 6.3 CAN 参数
- **驱动接口**：决定使用哪套 CAN 后端
  - `zlgcan`：周立功/创芯 USBCAN 适配器，走 ControlCAN.dll（VCI_* 老接口）
  - `zlgcan-zcan`：致远原厂 zlgcan.dll（ZCAN_* 新接口）
  - 其余（slcan / pcan / kvaser 等）：走 python-can 库
- **设备类型 / 设备索引 / 通道**：选 zlgcan 系后端时出现，按适配器型号与接线填写
- **通道**：can0 / can1（按硬件填写）
- **波特率**：125 K / 250 K / 500 K / 1 M

> 使用周立功适配器时，需保证根目录 `ControlCAN.dll` 或 `zlgcan_x64/` 驱动文件夹随程序分发。

### 6.4 操作
- “连接 / 断开”按钮切换连接状态
- 状态指示：已连接 / 未连接
- 数据收发测试：可在收发窗口手动发送字节并查看返回
- 错误日志：实时显示通信错误、超时与重连记录

---

## 七、模型训练页面

### 7.1 数据采集
1. 选择数据标签：正常 (0.0) / 警告 (0.5) / 异常 (1.0)
2. 设置采集时长（秒）
3. 点击“开始采集”，软件以监控页相同节奏写入特征行，到达时长后自动停止
4. 也可点击“导入 CSV”加载历史数据
5. “保存数据集”将当前累计数据写入 `training/data/dataset_<时间戳>.csv`

特征列固定为 8 维：
`speed_actual, speed_target, current_actual, current_target,`
`torque_actual, torque_target, angle_actual, temperature` + `label`

### 7.2 数据清洗
勾选要执行的步骤后点击“执行清洗”：
- 去重（相邻完全相同行）
- 归一化（Z-score）
- 删除含 NaN/Inf 行
- 转速范围过滤（按 `speed_actual` 列裁剪）

### 7.3 模型选择与超参数
**模型类型**（下拉框）：

| 模型 | 结构超参数 |
| --- | --- |
| MLP (多层感知机) | 隐藏维度、隐藏层数、Dropout |
| 1D-CNN (一维卷积) | 通道数、卷积核大小、Dropout |
| LSTM (长短时记忆) | 隐藏维度、LSTM 层数、Dropout |
| Transformer | d_model、注意力头数、Encoder 层数、Dropout |
| 随机森林 (Random Forest) | 树数量、最大深度 |
| 支持向量机 (SVM) | C、核函数（rbf/linear/poly/sigmoid）、gamma |

**任务类型**：回归（异常分数）/ 二分类。

**通用训练超参数**：

| 项 | 默认值 |
| --- | --- |
| 训练轮数 (Epochs) | 50 |
| 学习率 (LR) | 1e-3 |
| 批大小 (Batch) | 32 |
| 验证集比例 | 0.2 |
| 权重衰减 (L2) | 0 |
| 优化器 | Adam / SGD / RMSprop / AdamW |
| 损失函数 | MSELoss / L1Loss / BCELoss / CrossEntropy |
| 学习率调度 | None / StepLR / CosineAnnealingLR / ReduceLROnPlateau |
| 随机种子 | 42 |

> 选择随机森林 / SVM 时，训练轮数、学习率、优化器等深度学习专属项会自动置灰。

### 7.4 训练流程
1. 完成数据清洗（≥ 10 条数据）
2. 选择模型类型并填写超参数
3. 点击“开始训练”→ 后台线程训练，实时刷新 Loss 曲线与日志
4. 如需提前结束，点击“中止训练”
5. 训练完成后“导出 ONNX”按钮亮起（仅 PyTorch 模型支持），点击保存 `.onnx` 文件
6. 导出的 ONNX 可直接到“边缘 AI”页面加载使用

> 训练曲线区会同时绘制 Train Loss 与 Val Loss，便于发现过拟合。

---

## 八、AI 分析与边缘 AI

### 8.1 AI 分析（在线大模型）
- 在 AI 分析页填入 Base URL（默认 `https://api.openai.com/v1`）、API Key、模型名（默认 `gpt-4o`）
- 点击“分析当前状态”，软件将最近的遥测帧整理为 Prompt 调用大模型，返回自然语言诊断
- **图片理解**：点击"添加图片"附加本地图片，或一键抓取监控页波形截图，
  交给支持视觉输入的大模型（如 qwen-vl 系列）判断运行状态与故障迹象

### 8.2 边缘 AI
- 加载本地 ONNX 模型（来自模型训练页导出，或第三方训练）
- 实时把遥测数据喂入模型，显示异常分数；超出阈值时给出告警

---

## 九、操作记录页面

汇总所有关键操作的时间、动作、详情，可滚动浏览，文件落盘 `logs/operation_log.txt`。
便于事后复盘与故障定位。

---

## 十、典型工作流

### 10.1 PMSM 调试
1. 通信设置 → 连接串口
2. 电机控制 → 选择 PMSM、勾选 QEP + Hall、控制方式选 闭环 PI
3. 设置 Kp/Ki，目标转速 1500 rpm，点击启动
4. 切到监控页观察跟随效果
5. 调参后回到控制页“保存/应用参数”

### 10.2 双凸极电机低速重载
1. 控制页电机类型切到“双凸极电机”
2. 控制方式选“电流斩波控制 (CCC)”
3. 设置 i_upper=10 A、i_lower=8 A、斩波频率 10 kHz
4. 启动电机，监控电流波形

### 10.3 异常检测模型训练与部署
1. 模型训练页 → 采集数据（分别在正常/警告/异常工况）
2. 数据清洗
3. 选择 LSTM，隐藏维度 64、层数 2，优化器 AdamW、学习率 5e-4，调度器 CosineAnnealingLR，训练 100 轮
4. Loss 收敛后“导出 ONNX”
5. 切到“边缘 AI”页加载该模型 → 实时推理

---

## 十一、常见问题（FAQ）

**Q1. 启动时报缺少 PySide6 / pyqtgraph？**
请先 `pip install -r requirements.txt`。曲线区降级显示文字提示，但不影响主流程。

**Q2. 没有真实下位机能用吗？**
可以。在“电机控制”页点击“启动仿真”即可生成模拟遥测，验证 UI 与训练流程。

**Q3. 切换电机类型后控制方式列表为什么变了？**
这是设计行为。每种电机有适配的控制方式，例如 CCC/APC 仅对双凸极电机有意义。

**Q4. 训练随机森林为何不能导出 ONNX？**
当前 ONNX 导出仅支持 PyTorch 模型。可改用 MLP / CNN / LSTM / Transformer。

**Q5. 位置传感器为何允许多选？**
工业项目中常并联多个传感器互相校验或冗余兜底（如 QEP 主用 + Hall 兜底，
或低速时 SMO 切换到 HFI）。多选项会随参数一同下发到下位机。

**Q6. 紧急停止与停止有何区别？**
“停止电机”发送正常停机指令，下位机按减速曲线停车；
“紧急停止”立即切断输出并复位上位机控制器内部状态，用于异常工况。

---

## 十二、目录速查

| 路径 | 说明 |
| --- | --- |
| `main.py` | 程序入口 |
| `config/config.py` | 全部常量、参数 dataclass、命令字 |
| `pages/training_page.py` | 模型训练页面（含模型/超参选择） |
| `pages/control_page.py` | 电机控制页面（含传感器多选、SRM 控制方式） |
| `pages/vector_page.py` | 矢量可视化页面（电流圆 / 磁链圆） |
| `pages/identify_page.py` | 参数辨识页面（B / Tc / J） |
| `communications/motor_sim.py` | 数字孪生 L1 虚拟电机（PMSM dq 模型） |
| `communications/zlgcan_comm.py` | 周立功 CAN（ControlCAN.dll，VCI_* 接口） |
| `communications/zlgcan_zcan_comm.py` | 周立功 CAN（zlgcan.dll，ZCAN_* 接口） |
| `controllers/*.py` | 各控制算法实现 |
| `training/trainer.py` | 多模型训练器 |
| `logs/operation_log.txt` | 操作日志文件 |
| `training/data/` | 数据集导出目录 |
| `下位机适配/` | 下位机协议移植包（STM32 / C2000 / Arduino / FPGA） |

如需协助或反馈问题，请连同 `logs/operation_log.txt` 一并提供，便于快速定位。
