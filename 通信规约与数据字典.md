# 通信规约与数据字典

> 本文档集中说明上位机与下位机之间的**全部上下行数据**：哪些是上行遥测、哪些是下行控制、各自频率与字段、哪些参数**不会**下发，以及 0xF2 诊断帧里 PWM 比较值/采样点/扇区/VDDA 等底层字段的物理含义。
>
> 帧格式（v1/v2 帧头、CRC、序号、握手）见 [`下位机适配/PROTOCOL_V2.md`](下位机适配/PROTOCOL_V2.md)；本文档只描述**载荷语义**。代码与文档不一致时以代码为准

---

## 一、上下行总览

| 方向 | 命令字 | 名称 | 频率 | 触发 | 用途 |
|---|---|---|---|---|---|
| ↓ 上位机→下位机 | 0x10 | CMD_START | 事件 | 用户点"启动" | 启动电机 |
| ↓ | 0x11 | CMD_STOP | 事件 | 用户点"停止" | 正常停机 |
| ↓ | 0x12 | CMD_EMERGENCY_STOP | 事件 | 用户点"急停" | 立即封波 |
| ↓ | 0x13 | CMD_RESET_FAULT | 事件 | 故障复位按钮 | 清除故障锁定 |
| ↓ | 0x20 | CMD_SET_PARAMS | 事件 | "保存/应用参数" | 下发控制参数（文本键值对） |
| ↓ | 0x21 | CMD_SET_SENSOR | 事件 | "保存/应用参数" | 下发位置传感器配置 |
| ↓ | (v2) | HELLO / HEARTBEAT | 0.5 s 周期 | 自动 | 握手与心跳 |
| ↑ 下位机→上位机 | 0xF0 | 常规遥测 | 10 Hz | 周期上报 | 速度/温度/故障等慢变量 |
| ↑ | 0xF1 | 高速遥测 | 200 Hz（UART）/1 kHz（TCP 批） | 周期上报 | 电角度/Iq/相电流/Vd/Vq/Vbus |
| ↑ | 0xF2 | 电流采样诊断 | 50 Hz | 周期上报 | ADC 原始值/PWM duty/扇区/标定 |
| ↑ | (v2) | ACK / NACK | 事件 | 命令应答 | 命令成功/失败 |
| ↑ | (v2) | CAPABILITIES | 事件 | 握手响应 | 设备身份与能力表 |

**说明**：
- 上行三种遥测通道是**独立时钟、独立字段、独立回调**，时轴不严格对齐；`tick_ms` 是下位机 HAL_GetTick 毫秒时间戳，与上位机 `monotonic_s`、墙上时间 ISO 字符串分别保存。
- 所有下行命令都是**事件触发**（非周期），不会定时重发；ACK 超时由 v2 会话层处理。
- v1 协议只有 0x10/0x11/0x12/0x13/0x20/0x21 + 0x01 遥测，没有 0xF0/0xF1/F2 三通道区分；v1 把所有遥测统一打包成 15 字节（串口）或 8 字节（CAN）。
- v2 协议**经典 CAN 不支持**（无分片协议），CAN 上只能跑 v1 的 8 字节压缩遥测。

---

## 二、下行命令详细

### 2.1 CMD_START (0x10) — 启动电机

**payload 格式**（v1/v2 通用，UTF-8 文本键值对，分号分隔）：

```
target=<rpm>;[max_rpm=<rpm>]
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `target` | float | 是 | 目标转速 rpm |
| `max_rpm` | float | 否 | 启动时附带最高转速限制（可选） |

**仿真模式**：上位机解析 `target` 字段后调用 `MotorSim.start(target)`，虚拟电机进入运行态。
**真机模式**：下位机收到后按自身固件逻辑启动，ACK 成功后上位机状态机进入 RUNNING。

### 2.2 CMD_STOP (0x11) / CMD_EMERGENCY_STOP (0x12)

无 payload（空字节）。两个命令都是**安全相关**：

- **CMD_STOP**：正常停机，下位机按减速曲线停车。
- **CMD_EMERGENCY_STOP**：立即封波，输出归零，电机自由滑行。
- **v2 安全例外**：这两个命令**在会话未就绪时也允许发送**（`sequence=0xFFFF`），保证断链情况下仍能发出保命指令；其它命令必须会话 READY。

### 2.3 CMD_RESET_FAULT (0x13)

无 payload。仅 v2 协议使用；用于清除 `FAULT_LOCKED` 状态。下位机必须校验：
- 保护条件未清除（过流/过压/驱动故障仍存在）→ **必须 NACK**
- 硬件故障已清除 → ACK，回到允许预检状态

### 2.4 CMD_SET_PARAMS (0x20) — 下发控制参数

**这是最重要的下行命令**。payload 是 UTF-8 文本键值对，分号分隔：

```
motor=<电机类型>;mode=<控制方式>;sensors=<传感器列表>;<参数键>=<值>;...;max_current_a=<值>
```

#### 实际下发的字段（按控制方式）

**闭环 PI 控制（PIPanel）**：

| 键 | 说明 | 是否真机解析 |
|---|---|---|
| `kp_spd` | 速度环比例增益 | ✅ 真机速度 PI 用 |
| `ki_spd` | 速度环积分增益 | ✅ 真机速度 PI 用 |
| `kd_spd` | 速度环微分增益 | ⚠️ 真机一般不用，仅上位机保留 |
| `iq_max` | q 轴电流限幅 A | ✅ 真 Iq 限流用 |
| `spd_sample_time` | 速度环采样时间 s | ✅ 真机采用 |
| `kp_cur` | 电流环比例增益 | ✅ 真机电流 PI 用 |
| `ki_cur` | 电流环积分增益 | ✅ 真机电流 PI 用 |
| `cur_sample_time` | 电流环采样时间 s | ✅ 真机采用 |
| `kp`/`ki`/`kd`/`sample_time` | 兼容旧协议键，映射为外环参数 | 兼容字段 |

**开环控制（OpenLoopPanel）**：

| 键 | 说明 | 是否真机解析 |
|---|---|---|
| `control_mode` | `"current_loop_test"` 标识 | ✅ |
| `iqref` | q 轴电流给定 A（速度环旁路） | ✅ |
| `ramp_time` | Iqref 斜坡时间 s | ✅ |

> 开环模式不是 V/f 开环，而是**速度环旁路、电流环保持闭环**，用于电流环 PI 整定。空载电机会持续加速，超过 1500 rpm 受控停机，超过 2500 rpm 跑飞锁存故障。

**模型预测控制（MPCPanel）**：

| 键 | 说明 | 是否真机解析 |
|---|---|---|
| `mpc_type` | MPC 子类型 | ⚠️ 真机一般不解析 |
| `prediction_horizon` | 预测时域 N | ⚠️ 真机一般不解析 |
| `control_horizon` | 控制时域 M | ⚠️ |
| `weight_q` | 状态权重 Q | ⚠️ |
| `weight_r` | 控制权重 R | ⚠️ |
| `u_max` / `u_min` | 控制量上下限 | ⚠️ |

> MPC 参数当前**上位机本地控制器使用**真机固件通常不解析；下位机仍按 PI 运行，MPC 仅做仿真演示。

**SRM 控制方式**：

| 控制方式 | 下发键 | 真机解析 |
|---|---|---|
| 电流斩波 (CCC) | `current_upper`, `current_lower`, `chop_freq`, `hysteresis` | ✅ |
| 角度位置 (APC) | `turn_on_angle`, `turn_off_angle`, `advance_angle`, `current_limit` | ✅ |
| 电压 PWM | `dc_bus_voltage`, `duty`, `pwm_freq`, `voltage_limit` | ✅ |

#### 元数据字段（每次都发，真机用于日志）

| 键 | 说明 |
|---|---|
| `motor` | 电机类型字符串（PMSM/SRM/直线电机） |
| `mode` | 控制方式字符串 |
| `sensors` | 已选传感器列表，竖线分隔（`QEP|Hall`） |

#### 全局保护字段（每次都发）

| 键 | 说明 | 真机解析 |
|---|---|---|
| `max_current_a` | 电流限幅 A | ✅ 真机 Iq 限流（与 `iq_max` 重复但优先级更高） |

#### **不下发**的参数

以下参数**仅在上位机本地使用，不下发到下位机**：

| 参数 | 在哪用 | 为什么不下发 |
|---|---|---|
| `MotorConfig.max_rpm`（界面最高转速限制） | 上位机 UI 校验、数字孪生 | 真机有自己的固件上限，不需要上位机告诉 |
| `MotorConfig.rated_temperature`（额定工作点温度） | 设备档案、实验保护快照 | 是铭牌属性，不参与实时控制 |
| `MPCParams`（预测时域/权重） | 上位机 `MPCController` 本地 | 真机固件不实现 MPC，下位机按 PI 运行 |
| 数字孪生 `PMSMParams`（Rs/Ld/Lq/ψf/J/B/T_coulomb/T_cogging） | `MotorSim` 仿真 | 是仿真模型参数，真机有自己的物理参数 |
| 母线/热模型参数（`r_src`/`c_bus`/`r_brake`/`v_brake_*`/`rth`/`tau_th`） | `MotorSim` 仿真 | 同上 |
| 辨识结果（B/Tc/J） | 写回 `PMSMParams` | 仿真用，不下发 |
| 实验档案/模板字段 | `experiments/` 持久化 | 上位机本地记录，不参与控制 |
| 设备身份白名单（device_id/hardware_version/firmware_prefix） | 握手校验 | 仅用于 v2 HELLO/CAPABILITIES 比对 |

### 2.5 CMD_SET_SENSOR (0x21) — 下发位置传感器配置

**payload 格式**（二进制 + 文本混合）：

```
[1 字节 sensor_id][UTF-8 文本键值对，分号分隔]
```

示例：

```
0x01  lines=1000;phase=AB;index=Z
       └ QEP sensor_id=1 ┘  └ QEP 参数 ┘
```

每种传感器的参数键（来自 `SENSOR_REGISTRY[...].params_cls`）：

| 传感器 | sensor_id | 参数键 |
|---|---|---|
| Hall | 0 | `phase_sequence`, `pole_pairs` |
| QEP | 1 | `lines_per_rev`, `phase`, `index` |
| Resolver | 2 | `pole_pairs`, `excitation_freq`, `carrier_ratio` |
| SMO | 3 | `gain_k`, `cutoff_hz`, `startup_speed` |
| EKF | 4 | `q_noise`, `r_noise`, `p0` |
| MRAS | 5 | `adapt_gain`, `cutoff_hz` |
| HFI | 6 | `inj_amp`, `inj_freq`, `demod_cutoff` |

**CAN ID**：使用 `SENSOR_REGISTRY[sensor].can_id_default`（Resolver=0x201，其它=0x100）。

**发送顺序**：`_on_apply` 先发 `CMD_SET_SENSOR`，再发 `CMD_SET_PARAMS`，保证下位机先切好观测器再应用控制参数。

---

## 三、上行遥测详细

### 3.1 0xF0 常规遥测帧（10 Hz，慢变量）

**串口/TCP payload 15 字节**，struct 格式 `<hHhHhbBBBx`：

| 偏移 | 类型 | 字段 | 换算 | 说明 |
|---|---|---|---|---|
| 0 | int16 | speed_actual | rpm 直接读 | 实际转速（有符号） |
| 2 | uint16 | speed_target | rpm 直接读 | 目标转速 |
| 4 | int16 | current_actual | `A = raw / 100` | q 轴电流 mA |
| 6 | uint16 | angle_raw | 见 SENSOR_REGISTRY | 传感器原始值（Hall 步数/QEP 脉冲） |
| 8 | int16 | angle_actual | `deg = raw / 100` | 角度 0.01° |
| 10 | int8 | temperature | `°C = raw - 40` | 温度（+40 偏置，-40~127°C 量程） |
| 11 | uint8 | sensor_quality | 0..255 | 255=最佳 |
| 12 | uint8 | convergence | 0..255 | 255=完全收敛 |
| 13 | uint8 | flags | 位掩码 | bit0=低速警告, bit1=过流, bit2=驱动故障, bit3=急停 |
| 14 | uint8 | padding | 0 | 填充，保持 15 字节对齐 |

**CAN payload 8 字节**，struct 格式 `<hHhH`：只含 speed_actual / speed_target / current_actual / angle_raw 四个核心字段，省略温度/质量/收敛度/flags。上位机沿用上一帧补全缺失字段。

### 3.2 0xF1 高速遥测帧（200 Hz / 1 kHz，电流环级）

载荷可包含 1 个或多个样本，按 `payload_length` 整除性判定单样本字节数：

| payload_length | 单样本字节 | 内容 |
|---|---|---|
| 12 的倍数 | 12 | 精简帧：tick + 角度 + 速度 + Iq + Iqref |
| 16 的倍数 | 16 | 标准帧：精简 + Ia + Ib（相电流） |
| 22 的倍数 | 22 | 扩展帧：标准 + Vd + Vq + Vbus（施加电压与母线） |

**单样本布局**（小端序）：

| 偏移 | 字节 | 类型 | 字段 | 换算 |
|---|---|---|---|---|
| 0 | 4 | uint32 | tick_ms | 下位机 HAL_GetTick 毫秒 |
| 4 | 2 | uint16 | angle_raw | `deg = raw × 360 / 65536` |
| 6 | 2 | int16 | speed_rpm | 直接读 |
| 8 | 2 | int16 | iq_raw | `A = raw × 0.000629` |
| 10 | 2 | int16 | iqref_raw | `A = raw × 0.000629` |
| 12 | 2 | int16 | ia_raw | `A = raw × 0.000629`（16/22 字节帧） |
| 14 | 2 | int16 | ib_raw | `A = raw × 0.000629`（16/22 字节帧） |
| 16 | 2 | int16 | vd_raw | MCSDK s16 码值，保留原始（22 字节帧 |
| 18 | 2 | int16 | vq_raw | MCSDK s16 码值，保留原始（22 字节帧） |
| 20 | 2 | uint16 | vbus_v | 母线电压，伏特整数（22 字节帧） |

**频率规则**（代码 `comm_manager.py:620`）：

```
if len(chunks) > 1 或介质 == "以太网TCP":
    rate_hz = 1000      # 批量样本按 1 kHz 标度
else:
    rate_hz = 200       # UART 单样本按 200 Hz
```

> `0.000629` 是 MCSDK 默认 s16→A 标度，对应 ADC 满量程 + 运放增益组合。若下位机改采样电阻或运放增益，必须同步修改 `comm_manager.py` 解码常量。
>
> `vd_raw` / `vq_raw` 是 MCSDK 内部 s16 码值（PI 输出的施加电压），**不直接换算为伏特**；离线用堵转 R·Iq 自标定。`vbus_v` 是真实伏特整数。

### 3.3 0xF2 电流采样诊断帧（50 Hz，ADC/PWM 诊断）

载荷长度 21 / 29 / 31 字节三档：

| 长度 | 内容 |
|---|---|
| 21 | 基础：tick + ADC + offset + sector + duty + sample_point |
| 29 | 基础 + 4 个校准窗口字段 |
| 31 | 基础 + 校准 + VDDA 实测 |

**21 字节基础布局**（小端序，struct `<IHHHHBHHHH`）：

| 偏移 | 字节 | 类型 | 字段 | 含义 |
|---|---|---|---|---|
| 0 | 4 | uint32 | tick_ms | 下位机毫秒时间戳 |
| 4 | 2 | uint16 | adc1_raw | A 相 ADC 注入采样原始码 |
| 6 | 2 | uint16 | adc2_raw | B 相 ADC 注入采样原始码 |
| 8 | 2 | uint16 | offset_a | A 相零点（**MCSDK 已翻倍**） |
| 10 | 2 | uint16 | offset_b | B 相零点（同上） |
| 12 | 1 | uint8 | sector | SVPWM 当前扇区 0..5 |
| 13 | 2 | uint16 | duty_a | A 相 PWM 比较值（CCR 寄存器原始码） |
| 15 | 2 | uint16 | duty_b | B 相 PWM 比较值 |
| 17 | 2 | uint16 | duty_c | C 相 PWM 比较值 |
| 19 | 2 | uint16 | sample_point | ADC 注入触发点（计数值） |

**29 字节扩展**：在 21 字节后追加：

| 偏移 | 字节 | 类型 | 字段 | 含义 |
|---|---|---|---|---|
| 21 | 2 | uint16 | cal_adc1_min | A 相 ADC 校准窗口下限 |
| 23 | 2 | uint16 | cal_adc1_max | A 相 ADC 校准窗口上限 |
| 25 | 2 | uint16 | cal_adc2_min | B 相 ADC 校准窗口下限 |
| 27 | 2 | uint16 | cal_adc2_max | B 相 ADC 校准窗口上限 |

**31 字节扩展**：在 29 字节后追加：

| 偏移 | 字节 | 类型 | 字段 | 含义 |
|---|---|---|---|---|
| 29 | 2 | uint16 | vdda_mv | VREFINT 换算的实际 VDDA（mV）；0=固件尚未完成首次测量 |

---

## 四、0xF2 字段物理含义详解

这是容易混淆的部分，单独展开。

### 4.1 ADC 原始码与零点偏置

**STM32F4 注入组 ADC 特性**：
- 12 位 ADC 结果**左对齐至 16 位**，bit15 用作符号位；
- 有效范围 `0..32760`（不是 0..65535），中点约 16384（对应 1.65 V，零电流）；
- MCSDK 把结果**翻倍**后再做 offset 减法，因此 `offset_*` 字段会出现 > 32768 的值（如 32840），这是协议特性**不是溢出**。

**电压换算**（写死在 `comm_manager.py:668`）：

```
adc_volts_per_count = 3.3 / 32768
adc_v   = adc_raw  × adc_volts_per_count       # 单端电压
zero_v  = (offset_raw / 2) × adc_volts_per_count  # 零点电压（先除2还原）
```

**电流换算**（`comm_manager.py:669-676`）：

```
amps_per_mc_digit = 3.3 / (65536 × 0.01 × 8)
adc1_delta_a = (offset_a - 2 × adc1_raw) × amps_per_mc_digit
adc2_delta_a = (offset_b - 2 × adc2_raw) × amps_per_mc_digit
```

其中：
- `0.01` = 采样电阻 0.01 Ω（**硬件常量，必须与下位机一致**）；
- `8` = 运放增益 8 倍（**硬件常量，必须与下位机一致**）；
- 公式里的 `2 ×` 是因为 MCSDK 把 ADC 结果和 offset 都翻倍了，相减时翻倍抵消。

> 若下位机改动采样电阻或运放增益，上位机读出的 A 相电流会**系统性偏差**。这两个常量写在代码里，不通过协议协商。

### 4.2 PWM 比较值（duty_a / duty_b / duty_c）

这是 STM32 高级定时器（TIM1）的**比较寄存器（CCR）原始码**，不是占空比百分比。

**量纲**：
- 原始码 0..TIM1_ARR（通常 5249 或类似值），对应占空比 0..100%；
- 例：若 ARR=5249，则 duty=2625 ≈ 50% 占空比；
- 上位机不做换算，只存原始码，诊断页用相对值显示。

**含义**：
- CCR 决定每相 PWM 高电平宽度，三相 CCR 配合 SVPWM 算法产生正弦电流；
- 正常运行时三相 duty 应在 ARR/2 附近波动（约 50% 占空比中心），波动幅度对应调制深度；
- 三相 duty 之和约等于 3×ARR/2（SVPWM 零序注入）；
- **如果三相 duty 偏离中心很多或卡在 0/ARR 边界，说明调制饱和或 PWM 异常**。

### 4.3 采样点（sample_point）

这是 ADC 注入触发的**计数值**，对应 PWM 周期内的某个时刻。

**含义**：
- STM32 用定时器 TRGO 事件触发 ADC 注入采，TRGO 由比较匹配或更新事件产生；
- `sample_point` 是触发时刻在 PWM 周期内的位置（计数器值）；
- 通常设在 PWM 周期中点（计数器到达顶点或底点）采样，此时电流最稳定、纹波最小；
- 本项目数据 `sample_point=5249` 恒定不变，说明固件**固定在周期中点采样**，没有做自适应采样点调整；
- 若下位机改成在 PWM 边沿采样或在多个点采样，这个值会变化。

### 4.4 扇区（sector）

SVPWM 六扇区编号 0..5，对应三相电压矢量的六个非零矢量 V1..V6。

**含义**：
- 每个电周期内扇区应该**循环 0→1→2→3→4→5→0**，每个扇区持续 60° 电角度；
- 一个电周期 = 360° 电角度 = 6 个扇区；
- 极对数 4 时，一个机械转 = 4 个电周期 = 24 次扇区切换。

**正常数据特征**：
- 高速运行时 50 Hz 采样应能采到所有 0..5 扇区，分布相对均匀（每个约 16.7%）；
- 低速时一个扇区可能持续多个采样周期，分布不均匀是正常的。

**异常诊断**：
- 如果 sector 恒为某个固定值（如项目历史数据中 sector=4 恒定），说明：
  1. 下位机扇区识别只用了一相/一路霍尔，或
  2. 诊断帧只发了当前最后一个扇区的样本，没轮询全扇区，或
  3. 触发条 `sample_point` 固定不变，正好始终落在同一扇区窗口内。
- 这种情况下 `cal_adc_pp`（标定窗口）会**小于实测电流 PP**，因为只采到一个扇区的电流摆幅。

### 4.5 校准窗口（cal_adc1_min/max, cal_adc2_min/max）

**含义**：
- 下位机上电自检时让 ADC 在所有扇区扫一遍，记录每相 ADC 的最小值和最大值；
- `cal_pp = max - min` 是该相 ADC 在全扇区扫描中的峰峰值；
- 用于上位机判断**标定是否覆盖实测电流摆幅**。

**校验规则**：
- 正常情况下 `cal_pp ≥ 实测 ADC PP`（实测 PP = `max(adc_raw) - min(adc_raw)`）；
- 如果 `cal_pp < 实测 PP`，说明标定时电机没转到所有扇区，标定无效；
- 项目历史数据中 cal_pp 普遍只有实测 PP 的 46~69%，与 sector 锁定问题同源。

### 4.6 VDDA（vdda_mv，31 字节扩展帧）

**含义**：
- STM32 内置 VREFINT 通道，ADC 读 VREFINT 后反推实际 VDDA（标称 3300 mV）；
- VDDA 会随温度、负载、LDO 纹波漂移，影响 ADC 标度；
- 上位机用实际 VDDA 替代标称 3.3 V 重新换算电流，提高精度；
- `0` 表示固件尚未完成首次 VREFINT 测量，上位机仍按标称 3.3 V 计算。

---

## 五、TelemetryFrame 全字段（上位机内部对象）

`comm_manager.py:TelemetryFrame` 是 0xF0 解码后的内部对象，UI 各页都消费它。v2 0xF0 用 JSON 承载相同字段集：

| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| speed_actual | float | 0xF0 | 实际转速 rpm |
| speed_target | float | 0xF0 | 目标转速 rpm |
| current_actual | float | 0xF0 | q 轴电流 A（raw/100） |
| current_target | float | 0xF0 | q 轴电流给定 A |
| torque_actual | float | 0xF0 | 实际转矩 N·m（= current × 0.05） |
| torque_target | float | 0xF0 | 目标转矩 N·m |
| angle_actual | float | 0xF0 | 角度 deg（raw/100） |
| temperature | float | 0xF0 | 温度 °C（raw-40） |
| vdc | float | 0xF0 / 0xF1 扩展 | 直流母线电压 V |
| bus_state | str | 0xF0 | `"normal"`/`"brake"`/`"uv"`/`"ov"` |
| powers | dict | MotorSim 计算 | 功率流快照（W），真机暂为空 |
| sensor_source | str | 0xF0 | 当前活跃传感器名 |
| sensor_quality | float | 0xF0 | 0..1 数据质量 |
| angle_raw | float | 0xF0 | 传感器原始量 |
| convergence | float | 0xF0 | 0..1 观测器收敛度 |
| low_speed_warn | bool | 0xF0 flags bit0 | 低速段是否不可用 |
| fault_code | int | 0xF0 | 故障位掩码，0=无锁定故障 |
| fault_text | str | 上位机解析 | 可读故障原因 |
| mc_state | int | 0xF0 | MCSDK 状态机原始值 |
| max_rpm | float | 0xF0 | 下位机实际采用的最高转速 |
| current_limit_a | float | 0xF0 | 下位机实际采用的 Iq 限流 |
| runaway_limit_rpm | float | 0xF0 | 当前动态跑飞阈值 |
| phase_current_a | float | 0xF0 | 相电流幅值 A |
| speed_kp | int | 0xF0 | 下位机速度环实际 Kp 数字量（**回读**） |
| speed_ki | int | 0xF0 | 下位机速度环实际 Ki 数字量（**回读**） |
| data_source | str | 上位机标记 | `"sim"`/`"real"`/`"real_partial"` |

### 5.1 MCSDK 状态机 mc_state 取值

| 值 | 名称 | 含义 |
|---|---|---|
| 0 | IDLE | 空闲，未启动 |
| 1 | ALIGNMENT | 校准阶段 |
| 2 | STARTUP | 启动阶段 |
| 3 | RUN | 正常运行（部分固件版本） |
| 6 | RUN | 正常运行（本项目固件使用） |
| 11 | FAULT_OVER | 过压故障 |
| 12 | FAULT_OVER_CURRENT | 过流故障 |
| 13 | FAULT_OVER_TEMP | 过温故障 |
| 其它 | FAULT_* | 各类故障锁定 |

上位机 `_on_runtime_telemetry` 检测 `mc_state==6` 表示 RUN，从 RUN/STOPPING 转回 READY 表示设备主动受控停机。

### 5.2 回读字段说明

`max_rpm` / `current_limit_a` / `runaway_limit_rpm` / `speed_kp` / `speed_ki` 是**下位机回读**字段：
- 上位机下发 SET_PARAMS 后，下位机按自身策略调整这些值（可能因硬限制而改变），然后通过 0xF0 回传**实际采用值**；
- 上位机 UI 显示回读值，而不是下发值，避免误导操作员；
- `speed_kp` / `speed_ki` 是下位机 PI 控制器的**实际数字增益**，与上位机面板输入的浮点 Kp/Ki 是不同量纲，固件内部有定标。

---

## 六、v2 握手与心跳

### 6.1 HELLO / CAPABILITIES 握手

```
上位机                                  下位机
  │ HELLO(protocol_min=2, max=2)           │
  ├──────────────────────────────────────>│
  │                                       │ 校验版本与设备配置
  │ CAPABILITIES                           │
  │ device_id / firmware / hardware       │
  │ protocol_min/max / commands / fields  │
  │<──────────────────────────────────────┤
  │                                       │
  │ 版本交集 → READY                      │
  │ 无交集 → INCOMPATIBLE，禁止发命令     │
```

**设备身份白名单**（`configure_expected_device_identity`）：
- `device_id`、`hardware_version`、`firmware_prefix` 三个字段；
- CAPABILITIES 返回值必须**全部匹配**任一不符立即拒绝连接；
- **不降级到 v1**，直接抛异常。

### 6.2 HEARTBEAT 心跳

- 周期 **0.5 s**（联调初值，需按控制板负载重新标定）；
- 应答超时 **3 s**；
- 心跳同样分配序号并要求设备 ACK；
- 运行中若高速/常规遥测仍连续到达，遥测作为更强的在线证据；单独丢失
  心跳 ACK 只计入链路统计，不使会话失效，也不触发 `FAULT_LOCKED`；
- 只有遥测与心跳 ACK 同时消失才按真实断链处理。下位机 RUN 状态存在
  5 s 通信看门狗，因此不能在启动 3 s 后彻底停止发送上行保活数据；
- 设备重启后握手状态清除，旧会话的下一次心跳会收到 NACK 或超时；
- 一旦会话失效，上位机立即退出 READY，清空全部待 ACK 命令；
- 若当时处于 RUNNING，运行状态机进入 FAULT_LOCKED。

---

## 七、协议统计（`protocol_status()`）

通信页只读显示，用于实验后追溯链路质量，**不是实时保护量**：

| 字段 | 含义 |
|---|---|
| `tx_frames` | 累计发送帧数 |
| `rx_frames` | 累计接收帧数 |
| `telemetry_frames` | 遥测帧数（0xF0/F1/F2） |
| `crc_or_frame_errors` | CRC 错或帧结构错（含长度不合法） |
| `acks` / `nacks` | 命令应答计数 |
| `timeouts` | 命令超时计数 |
| `late_or_duplicate_acks` | 迟到或重复 ACK 计数 |
| `handshakes` | 握手次数 |
| `session_restarts` / `session_losses` | 会话重启/失效次数 |
| `pending_ack` | 当前待 ACK 命令数 |

---

## 八、CAN 总线特殊说明

### 8.1 CAN ID 分配

| 用途 | CAN ID | 方向 |
|---|---|---|
| 上位机下行默认 | 0x100 | 上机→下位机 |
| 旋转变压器遥测 | 0x201 | 下位机→上位机 |
| 其它传感器遥测 | 0x100 | 下位机→上位机 |

CAN ID 在 `_send_sensor_frame` 中按 `SENSOR_REGISTRY[sensor].can_id_default` 选取。

### 8.2 经典 CAN 限制

- 经典 CAN 单帧 payload 最多 8 字节；
- v2 最小帧（空 payload + 头 10B + CRC 2B + 尾 1B = 13B）已超出，**无法承载**；
- 因此经典 CAN **只能跑 v1 的 8 字节压缩遥测**（4 个核心字段）；
- v2 在 CAN 上使用需要先定义分片协议（帧序号 + 分片序号 + 总长度 + 超时清理 + 乱序策略），目前未实现；
- CAN FD（64 字节 payload）理论上可承载 v2 短帧，但当前驱动未启用 CAN FD。

---

## 九、字段在 UI 各页的分布

| UI 页 | 消费的字段 |
|---|---|
| 监控页 | speed_actual/target, current_actual, torque_actual, temperature, vdc, sensor_quality, fault_code/text |
| 电机控制页 | speed_kp/ki 回读, current_limit_a 回读, max_rpm 回读 |
| 矢量可视化页 | 0xF1 的 angle_raw, iq, ia, ib（仿真模式取 MotorSim 1 kHz trace） |
| 功率流页 | vdc, current_actual, MotorSim.powers（真机暂为空） |
| 参数辨识页 | speed_actual, current_actual（稳态两点 + 滑行拟合） |
| 电流采样诊断页 | 0xF2 全部字段（ADC/offset/sector/duty/sample_point/cal/vdda） |
| 边缘 AI 页 | speed_actual/target, current_actual/target, torque_actual/target, angle_actual, temperature |
| 操作记录页 | 不消费遥测，只显示操作日志 |
| 实验页 | TelemetryFrame 全部字段落盘到 telemetry.csv |

---

## 十、版本与代码位置

- 帧编解码：`communications/protocol.py`（v1）、`communications/protocol_v2.py`（v2）
- 三通道分发：`communications/comm_manager.py:590-700`（`_process_v2_responses`）
- TelemetryFrame 定义：`communications/comm_manager.py:45-101`
- 参数下发：`pages/control_page.py:750-816`（`_on_apply`、`_send_sensor_frame`）
- 各控制方式参数面板：`pages/control_param_panels/panels.py`
- 传感器注册表：`config/config.py:SENSOR_REGISTRY`
- 上位机版本：v1.8.0
- 最后更新：2026-07-20
