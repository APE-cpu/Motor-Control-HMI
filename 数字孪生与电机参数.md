# 数字孪生 L1 与电机参数说明

> 本文档集中描述 `communications/motor_sim.py` 中 PMSM dq 轴物理模型、双闭环 PI、母线动力学与热模型的全部默认参数。当前默认值已对齐**野火 42JSF840AS-1000-8 PMSM**（24V/4000rpm/4对极）铭牌参数。所有数值均以 SI 单位表示，便于与 datasheet 对应。本文档随代码同步，若与 `motor_sim.py` 不一致以代码为准。

## 一、模型总览

| 项 | 值 | 说明 |
|---|---|---|
| 模型类型 | PMSM 隐极/凸极 dq 轴模型 | 适用于表贴式与内置式 PMSM |
| 积分方法 | 定步长欧拉法 | 简单稳定，适合实时仿真 |
| 内部步长 `DT` | `5.0e-4` s（0.5 ms） | 与下位机 PWM 中断频率同量级 |
| 对外采样周期 | 0.1 s | 每 200 个内部步对外吐一帧遥测 |
| 高速轨迹频率 | 1 kHz | 每 2 个内部步采 1 次（`step % 2 == 0`） |
| 母线电压基准 | 24 V | 火 24V 平台 |
| 电流限幅 | 8 A | 额定电流约 4.5A，留 1.8× 余量 |

## 二、PMSMParams 默认值

### 2.1 电机本体参数（铭牌：野火 42JSF840AS-1000-8）

| 字段 | 默认值 | 单位 | 含义 | 铭牌来源 |
|---|---|---|---|---|
| `Rs` | 0.59 | Ω | 相电阻 | 铭牌 0.59±10% Ω |
| `Ld` | 0.66e-3 | H | d 轴电感 | 铭牌相电感 0.66±20% mH，表贴式 Ld≈Lq |
| `Lq` | 0.66e-3 | H | q 轴电感 | 同上，凸极率≈1 |
| `psi_f` | 7.04e-3 | Wb | 永磁磁链 | 由反电动势 2.95V/Krpm 换算 |
| `pole_pairs` | 4 | — | 极对数 | 铭牌 4 对极 |
| `J` | 1.85e-5 | kg·m² | 转动惯量 | 铭牌 1.85×10⁻⁵（仅转子，不含负载/联轴器） |
| `B` | 1.5e-4 | N·m·s/rad | 粘性摩擦系数 | 估算（小电机 1e-4 量级），建议辨识页实测 |
| `T_coulomb` | 0.01 | N·m | 库仑摩擦幅值 | 估算（约额定力矩 5%），建议辨识页实测 |
| `T_cogging` | 5e-3 | N·m | 齿槽转矩幅值（6 倍电角频率脉动） | 估算，建议实测 |

> **ψf 换算过程**：
> ```
> Ke = 2.95 V/Krpm（相反电动势峰值/每千转）
> at 4000 rpm: E_peak = 2.95 × 4 = 11.8 V
> ω_e = pole_pairs × ω_m = 4 × (4000 × 2π/60) = 1675.5 rad/s
> ψf = E_peak / ω_e = 11.8 / 1675.5 ≈ 0.00704 Wb
> ```
>
> **额定电流反推**：`i_rated = T_rated / (1.5 × p × ψf) = 0.19 / (1.5 × 4 × 0.00704) ≈ 4.5 A`

### 2.2 母线与电源参数（24V 平台）

| 字段 | 默认值 | 单位 | 含义 |
|---|---|---|---|
| `Vdc` | 24.0 | V | 电源空载电压（母线额定，铭牌 24V DC） |
| `i_max` | 8.0 | A | 电流限幅（额定 4.5A × 1.8） |
| `r_src` | 0.2 | Ω | 电源内阻（24V 小功率平台估算） |
| `c_bus` | 1.0e-3 | F | 母线电容（24V 平台估算 1 mF） |

### 2.3 制动斩波与保护阈值（24V 平台，按 48V 比例缩放）

| 字段 | 默认值 | 单位 | 含义 |
|---|---|---|---|
| `r_brake` | 10.0 | Ω | 制动斩波电阻（24V 小电机通常无斩波，保留模型结构） |
| `v_brake_on` | 27.0 | V | 斩波开启阈值（1.125×Vdc，滞回上限） |
| `v_brake_off` | 25.5 | V | 斩波关闭阈值（1.0625×Vdc，滞回下限） |
| `v_ov_trip` | 30.0 | V | 过压跳闸阈值（1.25×Vdc，封管保护，锁定） |
| `v_uv_warn` | 21.0 | V | 欠压告警阈值（0.875×Vdc） |

`bus_state` 只读属性按优先级返回：`ov_trip > brake > uv_warn > normal`。

### 2.4 热模型（一阶 RC，估算值）

| 字段 | 默认值 | 单位 | 含义 |
|---|---|---|---|
| `rth` | 3.0 | K/W | 热阻（小电机散热面积，估取 3 K/W） |
| `tau_th` | 120.0 | s | 热时间常数（小电机热容小但散热慢，估取 120s） |
| `t_amb` | 25.0 | °C | 环境温度 |

> 一阶热模型仅计铜损发热，不含铁损与开关损耗；适用于趋势性温度估计，不替代热成像或额定温升实验。rth/tau_th 为估算值，建议实测温升曲线后回写。

## 三、内嵌双闭环 PI

### 3.1 速度环

| 参数 | 值 | 说明 |
|---|---|---|
| Kp | 0.06 | 比例增益（小 J 电机对增益不敏感，沿用原值） |
| Ki | 2.0 | 积分增益（较 48V 平台 1.0 上调，补偿小惯量下的稳态精度） |
| 抗饱和 | 饱和时回扣 `err*dt` | 反算式（back-calculation） |

### 3.2 电流环

| 参数 | 值 | 说明 |
|---|---|---|
| Kp | 1.2 | 比例增益 |
| Ki | 300 | 积分增益 |
| 抗饱和 | 饱和时回扣 | 反算式 |
| 交叉耦合前馈 | 启用 | d/q 轴电压交叉项补偿 |

> 24V 平台电气时间常数 L/R ≈ 0.66mH/0.59Ω ≈ 1.1ms，电流环 Kp/Ki=1.2/300 在该 L/R 下仍稳定（原 48V 平台 L/R≈2ms 用同值，覆盖范围足够）。真机套用前建议先降低 20% 再现场整定。

## 四、控制接口

| 方法 | 作用 |
|---|---|
| `start()` | 进入运行态，启用 PI 闭环 |
| `stop()` | 正常停机（PI 闭环保持，目标转速归零） |
| `emergency_stop()` | 立即封波，输出归零 |
| `set_speed_target(rpm)` | 设置目标转速 |
| `set_load(N·m)` | 设置稳态机械负载 |
| `pulse_load(N·m, duration_s)` | 施加脉冲负载 |
| `set_load_disturbance(N·m, t_on, t_off)` | 周期性负载扰动 |

## 五、辨识页用到的派生常数

| 名称 | 公式 | 用途 |
|---|---|---|
| 转矩常数 Kt | `1.5 × pole_pairs × psi_f = 1.5 × 4 × 0.00704 ≈ 0.0423 N·m/A` | 稳态辨识 `Kt·iq = B·ω + Tc` |
| 反电动势峰值 | `psi_f × pole_pairs × ω_e` | ψf 反推实验 |

ψf 默认不参与自动辨识，需由铭牌或反电动势实验提供；仿真模式下可用上述默认值验证辨识精度。

## 六、与真机参数对齐步骤

1. 在辨识页完成两点稳态实验 → 得到 B、Tc；
2. 完成滑行实验 → 得到 J（注意铭牌给的 J 是转子，加上负载/联轴器后实测值会更大）；
3. 用铭牌或反电动势实验得到 ψf（铭牌已提供反电动势系数 2.95V/Krpm）；
4. 测量定子相电阻 Rs（万用表相间除2）与 Ld/Lq（LCR 表或高频注入法）；
5. 在 `PMSMParams` 中替换对应字段；
6. 重启仿真，对照矢量可视化页的电流圆/磁链圆形状确认模型与真机一致；
7. 真机套用 PI 参数前先降低 20%，逐步增大至稳定无超调。

## 七、版本与代码位置

- 代码文件：`communications/motor_sim.py`
- 参数 dataclass：`PMSMParams`
- 模型主类：`MotorSim`
- 上位机版本：v1.8.0
- 默认参数对齐：野火 42JSF840AS-1000-8 PMSM（24V/4000rpm/4对极）
- 最后更新：2026-07-20