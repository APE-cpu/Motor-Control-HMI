[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.12"]
build-backend = "scikit_build_core.build"

[project]
name = "motor-core-cpp"
version = "0.4.0"
description = "Native communication primitives for Motor Control HMI"
requires-python = ">=3.10"

[tool.scikit-build]
cmake.version = ">=3.20"
cmake.build-type = "Release"
wheel.packages = []
